---
ms.openlocfilehash: 181afe9acfe45ff619a50cf874669228b421b475
ms.sourcegitcommit: 7c550d2bcd30f505913e0cd441fbe12ae4a93b27
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 07/15/2021
ms.locfileid: "53445978"
---
<!-- markdownlint-disable MD002 MD041 -->

<span data-ttu-id="02afc-101">В этом упражнении вы завершите реализацию функций Azure и обновим тестового приложения, чтобы подписаться и отписаться от изменений в почтовом ящике `SetSubscription` `Notify` пользователя.</span><span class="sxs-lookup"><span data-stu-id="02afc-101">In this exercise you will finish implementing the Azure Functions `SetSubscription` and `Notify`, and update the test application to subscribe and unsubscribe to changes in a user's inbox.</span></span>

- <span data-ttu-id="02afc-102">Функция будет выступать в качестве API, позволяя тестовой приложению создавать или удалять подписку на изменения в почтовом `SetSubscription` ящике пользователя. [](https://docs.microsoft.com/graph/webhooks)</span><span class="sxs-lookup"><span data-stu-id="02afc-102">The `SetSubscription` function will act as an API, allowing the test app to create or delete a [subscription](https://docs.microsoft.com/graph/webhooks) to changes in a user's inbox.</span></span>
- <span data-ttu-id="02afc-103">Функция будет выступать в качестве веб-сайта, который получает уведомления об `Notify` изменении, созданные подпиской.</span><span class="sxs-lookup"><span data-stu-id="02afc-103">The `Notify` function will act as the webhook that receives change notifications generated by the subscription.</span></span>

<span data-ttu-id="02afc-104">Обе функции будут использовать поток клиентских учетных данных [для](https://docs.microsoft.com/azure/active-directory/develop/v2-oauth2-client-creds-grant-flow) получения маркера только для приложений для вызова Microsoft Graph.</span><span class="sxs-lookup"><span data-stu-id="02afc-104">Both functions will use the [client credentials grant flow](https://docs.microsoft.com/azure/active-directory/develop/v2-oauth2-client-creds-grant-flow) to get an app-only token to call Microsoft Graph.</span></span> <span data-ttu-id="02afc-105">Так как администратор предоставил администратору согласие на необходимые области разрешений, для получения маркера не потребуется взаимодействие с пользователем.</span><span class="sxs-lookup"><span data-stu-id="02afc-105">Because an administrator granted admin consent to the required permission scopes, no user interaction will be required to get the token.</span></span>

## <a name="add-client-credentials-authentication-to-the-azure-functions-project"></a><span data-ttu-id="02afc-106">Добавление проверки подлинности учетных данных клиентов в проект Azure Functions</span><span class="sxs-lookup"><span data-stu-id="02afc-106">Add client credentials authentication to the Azure Functions project</span></span>

<span data-ttu-id="02afc-107">В этом разделе вы реализуете поток клиентских учетных данных в проекте Azure Functions, чтобы получить маркер доступа, совместимый с Microsoft Graph.</span><span class="sxs-lookup"><span data-stu-id="02afc-107">In this section you'll implement the client credentials flow in the Azure Functions project to get an access token compatible with Microsoft Graph.</span></span>

1. <span data-ttu-id="02afc-108">Откройте CLI в каталоге, который содержит **GraphTutorial.csproj.**</span><span class="sxs-lookup"><span data-stu-id="02afc-108">Open your CLI in the directory that contains **GraphTutorial.csproj**.</span></span>

1. <span data-ttu-id="02afc-109">Добавьте свой ИД приложения webhook и секрет в секретный магазин с помощью следующих команд.</span><span class="sxs-lookup"><span data-stu-id="02afc-109">Add your webhook application ID and secret to the secret store using the following commands.</span></span> <span data-ttu-id="02afc-110">Замените `YOUR_WEBHOOK_APP_ID_HERE` iD приложения для **веб-Graph Azure Function.**</span><span class="sxs-lookup"><span data-stu-id="02afc-110">Replace `YOUR_WEBHOOK_APP_ID_HERE` with the application ID for the **Graph Azure Function Webhook**.</span></span> <span data-ttu-id="02afc-111">Замените секрет приложения, созданный на портале Azure для `YOUR_WEBHOOK_APP_SECRET_HERE` **веб-Graph Azure Function.**</span><span class="sxs-lookup"><span data-stu-id="02afc-111">Replace `YOUR_WEBHOOK_APP_SECRET_HERE` with the application secret you created in the Azure portal for the **Graph Azure Function Webhook**.</span></span>

    ```Shell
    dotnet user-secrets set webHookId "YOUR_WEBHOOK_APP_ID_HERE"
    dotnet user-secrets set webHookSecret "YOUR_WEBHOOK_APP_SECRET_HERE"
    ```

### <a name="create-a-client-credentials-authentication-provider"></a><span data-ttu-id="02afc-112">Создание поставщика проверки подлинности учетных данных клиента</span><span class="sxs-lookup"><span data-stu-id="02afc-112">Create a client credentials authentication provider</span></span>

1. <span data-ttu-id="02afc-113">Создайте новый файл в **каталоге ./GraphTutorial/Authentication** с именем **ClientCredentialsAuthProvider.cs** и добавьте следующий код.</span><span class="sxs-lookup"><span data-stu-id="02afc-113">Create a new file in the **./GraphTutorial/Authentication** directory named **ClientCredentialsAuthProvider.cs** and add the following code.</span></span>

    :::code language="csharp" source="../demo/GraphTutorial/Authentication/ClientCredentialsAuthProvider.cs" id="AuthProviderSnippet":::

<span data-ttu-id="02afc-114">Удумайте, что делает код **в ClientCredentialsAuthProvider.cs.**</span><span class="sxs-lookup"><span data-stu-id="02afc-114">Take a moment to consider what the code in **ClientCredentialsAuthProvider.cs** does.</span></span>

- <span data-ttu-id="02afc-115">В конструкторе инициализируется **конфиденциальнаяclientApplication** из `Microsoft.Identity.Client` пакета.</span><span class="sxs-lookup"><span data-stu-id="02afc-115">In the constructor, it initializes a **ConfidentialClientApplication** from the `Microsoft.Identity.Client` package.</span></span> <span data-ttu-id="02afc-116">Он использует `WithAuthority(AadAuthorityAudience.AzureAdMyOrg, true)` функции и функции, чтобы ограничить аудиторию входа только указанной Microsoft 365 `.WithTenantId(tenantId)` организации.</span><span class="sxs-lookup"><span data-stu-id="02afc-116">It uses the `WithAuthority(AadAuthorityAudience.AzureAdMyOrg, true)` and `.WithTenantId(tenantId)` functions to restrict the login audience to only the specified Microsoft 365 organization.</span></span>
- <span data-ttu-id="02afc-117">В `GetAccessToken` функции она вызывает `AcquireTokenForClient` получение маркера для приложения.</span><span class="sxs-lookup"><span data-stu-id="02afc-117">In the `GetAccessToken` function, it calls `AcquireTokenForClient` to get a token for the application.</span></span> <span data-ttu-id="02afc-118">Поток маркеров учетных данных клиента всегда не является интерактивным.</span><span class="sxs-lookup"><span data-stu-id="02afc-118">The client credentials token flow is always non-interactive.</span></span>
- <span data-ttu-id="02afc-119">Он реализует интерфейс, позволяя передавать этот класс в конструкторе исходяющих запросов для проверки `Microsoft.Graph.IAuthenticationProvider` `GraphServiceClient` подлинности.</span><span class="sxs-lookup"><span data-stu-id="02afc-119">It implements the `Microsoft.Graph.IAuthenticationProvider` interface, allowing this class to be passed in the constructor of the `GraphServiceClient` to authenticate outgoing requests.</span></span>

## <a name="update-graphclientservice"></a><span data-ttu-id="02afc-120">Обновление GraphClientService</span><span class="sxs-lookup"><span data-stu-id="02afc-120">Update GraphClientService</span></span>

1. <span data-ttu-id="02afc-121">Откройте **GraphClientService.cs** и добавьте следующее свойство в класс.</span><span class="sxs-lookup"><span data-stu-id="02afc-121">Open **GraphClientService.cs** and add the following property to the class.</span></span>

    :::code language="csharp" source="../demo/GraphTutorial/Services/GraphClientService.cs" id="AppGraphClientMembers":::

1. <span data-ttu-id="02afc-122">Замените имеющуюся функцию `GetAppGraphClient` указанным ниже кодом.</span><span class="sxs-lookup"><span data-stu-id="02afc-122">Replace the existing `GetAppGraphClient` function with the following.</span></span>

    :::code language="csharp" source="../demo/GraphTutorial/Services/GraphClientService.cs" id="AppGraphClientFunctions":::

## <a name="implement-notify-function"></a><span data-ttu-id="02afc-123">Реализация функции Notify</span><span class="sxs-lookup"><span data-stu-id="02afc-123">Implement Notify function</span></span>

<span data-ttu-id="02afc-124">В этом разделе будет реализована функция, которая будет использоваться в качестве URL-адреса уведомлений об `Notify` изменениях.</span><span class="sxs-lookup"><span data-stu-id="02afc-124">In this section you'll implement the `Notify` function, which will be used as the notification URL for change notifications.</span></span>

1. <span data-ttu-id="02afc-125">Создание нового каталога в **каталоге GraphTutorials** с именем **Models**.</span><span class="sxs-lookup"><span data-stu-id="02afc-125">Create a new directory in the **GraphTutorials** directory named **Models**.</span></span>

1. <span data-ttu-id="02afc-126">Создайте новый файл в **каталоге Models** с именем **ResourceData.cs** и добавьте следующий код.</span><span class="sxs-lookup"><span data-stu-id="02afc-126">Create a new file in the **Models** directory named **ResourceData.cs** and add the following code.</span></span>

    :::code language="csharp" source="../demo/GraphTutorial/Models/ResourceData.cs" id="ResourceDataSnippet":::

1. <span data-ttu-id="02afc-127">Создайте новый файл в **каталоге Models** с именем **ChangeNotificationPayload.cs** и добавьте следующий код.</span><span class="sxs-lookup"><span data-stu-id="02afc-127">Create a new file in the **Models** directory named **ChangeNotificationPayload.cs** and add the following code.</span></span>

    :::code language="csharp" source="../demo/GraphTutorial/Models/ChangeNotificationPayload.cs" id="ChangeNotificationSnippet":::

1. <span data-ttu-id="02afc-128">Создайте новый файл в **каталоге Моделей** с именем **NotificationList.cs** и добавьте следующий код.</span><span class="sxs-lookup"><span data-stu-id="02afc-128">Create a new file in the **Models** directory named **NotificationList.cs** and add the following code.</span></span>

    :::code language="csharp" source="../demo/GraphTutorial/Models/NotificationList.cs" id="NotificationListSnippet":::

1. <span data-ttu-id="02afc-129">Откройте **./GraphTutorial/Notify.cs** и замените все содержимое на следующее.</span><span class="sxs-lookup"><span data-stu-id="02afc-129">Open **./GraphTutorial/Notify.cs** and replace its entire contents with the following.</span></span>

    :::code language="csharp" source="../demo/GraphTutorial/Notify.cs" id="NotifySnippet":::

<span data-ttu-id="02afc-130">Удумайте, что делает код **в Notify.cs.**</span><span class="sxs-lookup"><span data-stu-id="02afc-130">Take a moment to consider what the code in **Notify.cs** does.</span></span>

- <span data-ttu-id="02afc-131">Функция `Run` проверяет наличие параметра `validationToken` запроса.</span><span class="sxs-lookup"><span data-stu-id="02afc-131">The `Run` function checks for the presence of a `validationToken` query parameter.</span></span> <span data-ttu-id="02afc-132">Если этот параметр присутствует, он обрабатывает запрос как запрос проверки [и](https://docs.microsoft.com/graph/webhooks#notification-endpoint-validation)отвечает соответствующим образом.</span><span class="sxs-lookup"><span data-stu-id="02afc-132">If that parameter is present, it processes the request as a [validation request](https://docs.microsoft.com/graph/webhooks#notification-endpoint-validation), and responds accordingly.</span></span>
- <span data-ttu-id="02afc-133">Если запрос не является запросом на проверку, полезной нагрузкой JSON является deserialized в `ChangeNotificationCollection` .</span><span class="sxs-lookup"><span data-stu-id="02afc-133">If the request is not a validation request, the JSON payload is deserialized into a `ChangeNotificationCollection`.</span></span>
- <span data-ttu-id="02afc-134">Каждое уведомление в списке проверяется на ожидаемое значение состояния клиента и обрабатывается.</span><span class="sxs-lookup"><span data-stu-id="02afc-134">Each notification in the list is checked for the expected client state value, and is processed.</span></span>
- <span data-ttu-id="02afc-135">Сообщение, которое вызвало уведомление, извлекалось с помощью Microsoft Graph.</span><span class="sxs-lookup"><span data-stu-id="02afc-135">The message that triggered the notification is retrieved with Microsoft Graph.</span></span>

## <a name="implement-setsubscription-function"></a><span data-ttu-id="02afc-136">Реализация функции SetSubscription</span><span class="sxs-lookup"><span data-stu-id="02afc-136">Implement SetSubscription function</span></span>

<span data-ttu-id="02afc-137">В этом разделе будет реализована функция SetSubscription.</span><span class="sxs-lookup"><span data-stu-id="02afc-137">In this section, you'll implement the SetSubscription function.</span></span> <span data-ttu-id="02afc-138">Эта функция будет выступать в качестве API, который вызван тест-приложением для создания или удаления подписки в почтовом ящике пользователя.</span><span class="sxs-lookup"><span data-stu-id="02afc-138">This function will act as an API that is called by the test application to create or delete a subscription on a user's inbox.</span></span>

1. <span data-ttu-id="02afc-139">Создайте новый файл в **каталоге Models** с именем **SetSubscriptionPayload.cs** и добавьте следующий код.</span><span class="sxs-lookup"><span data-stu-id="02afc-139">Create a new file in the **Models** directory named **SetSubscriptionPayload.cs** and add the following code.</span></span>

    :::code language="csharp" source="../demo/GraphTutorial/Models/SetSubscriptionPayload.cs" id="SetSubscriptionPayloadSnippet":::

1. <span data-ttu-id="02afc-140">Откройте **./GraphTutorial/SetSubscription.cs** и замените все содержимое следующим образом.</span><span class="sxs-lookup"><span data-stu-id="02afc-140">Open **./GraphTutorial/SetSubscription.cs** and replace its entire contents with the following.</span></span>

    :::code language="csharp" source="../demo/GraphTutorial/SetSubscription.cs" id="SetSubscriptionSnippet":::

<span data-ttu-id="02afc-141">Задумайтесь о том, что делает код **в SetSubscription.cs.**</span><span class="sxs-lookup"><span data-stu-id="02afc-141">Take a moment to consider what the code in **SetSubscription.cs** does.</span></span>

- <span data-ttu-id="02afc-142">Функция считывает полезной нагрузки JSON, отправленной в запрос POST, чтобы определить тип запроса (подписаться или отписаться), пользовательский ID для подписки и подписку на подписку, чтобы `Run` отписаться.</span><span class="sxs-lookup"><span data-stu-id="02afc-142">The `Run` function reads the JSON payload sent in the POST request to determine the request type (subscribe or unsubscribe), the user ID to subscribe for, and the subscription ID to unsubscribe.</span></span>
- <span data-ttu-id="02afc-143">Если запрос является запросом на подписку, он использует SDK microsoft Graph для создания новой подписки в почтовом ящике указанного пользователя.</span><span class="sxs-lookup"><span data-stu-id="02afc-143">If the request is a subscribe request, it uses the Microsoft Graph SDK to create a new subscription in the specified user's inbox.</span></span> <span data-ttu-id="02afc-144">Подписка будет уведомлять о том, когда сообщения создаются или обновляются.</span><span class="sxs-lookup"><span data-stu-id="02afc-144">The subscription will notify when messages are created or updated.</span></span> <span data-ttu-id="02afc-145">Новая подписка возвращается в полезной нагрузке JSON ответа.</span><span class="sxs-lookup"><span data-stu-id="02afc-145">The new subscription is returned in the JSON payload of the response.</span></span>
- <span data-ttu-id="02afc-146">Если запрос является отписаным, для удаления указанной подписки используется Graph microsoft Graph SDK.</span><span class="sxs-lookup"><span data-stu-id="02afc-146">If the request is an unsubscribe request, it uses the Microsoft Graph SDK to delete the specified subscription.</span></span>

## <a name="call-setsubscription-from-the-test-app"></a><span data-ttu-id="02afc-147">Вызов SetSubscription из тестового приложения</span><span class="sxs-lookup"><span data-stu-id="02afc-147">Call SetSubscription from the test app</span></span>

<span data-ttu-id="02afc-148">В этом разделе будут реализованы функции создания и удаления подписок в тестовом приложении.</span><span class="sxs-lookup"><span data-stu-id="02afc-148">In this section, you'll implement functions to create and delete subscriptions in the test app.</span></span>

1. <span data-ttu-id="02afc-149">Откройте **./TestClient/azurefunctions.js** и добавьте следующую функцию.</span><span class="sxs-lookup"><span data-stu-id="02afc-149">Open **./TestClient/azurefunctions.js** and add the following function.</span></span>

    :::code language="javascript" source="../demo/TestClient/azurefunctions.js" id="createSubscriptionSnippet":::

    <span data-ttu-id="02afc-150">Этот код вызывает функцию Azure для подписки и добавляет новую подписку в массив `SetSubscription` подписок в сеансе.</span><span class="sxs-lookup"><span data-stu-id="02afc-150">This code calls the `SetSubscription` Azure Function to subscribe and adds the new subscription to the array of subscriptions in the session.</span></span>

1. <span data-ttu-id="02afc-151">Добавьте следующую функцию **вazurefunctions.js.**</span><span class="sxs-lookup"><span data-stu-id="02afc-151">Add the following function to **azurefunctions.js**.</span></span>

    :::code language="javascript" source="../demo/TestClient/azurefunctions.js" id="deleteSubscriptionSnippet":::

    <span data-ttu-id="02afc-152">Этот код вызывает функцию Azure для отписки и удаляет подписку из массива `SetSubscription` подписок в сеансе.</span><span class="sxs-lookup"><span data-stu-id="02afc-152">This code calls the `SetSubscription` Azure Function to unsubscribe and removes the subscription from the array of subscriptions in the session.</span></span>

1. <span data-ttu-id="02afc-153">Если у вас нет запуска ngrok, запустите ngrok () и `ngrok http 7071` скопируйте URL-адрес переададки HTTPS.</span><span class="sxs-lookup"><span data-stu-id="02afc-153">If you do not have ngrok running, run ngrok (`ngrok http 7071`) and copy the HTTPS forwarding URL.</span></span>

1. <span data-ttu-id="02afc-154">Добавьте URL-адрес ngrok в хранилище секретов пользователей, выстроив следующую команду.</span><span class="sxs-lookup"><span data-stu-id="02afc-154">Add the ngrok URL to the user secrets store by running the following command.</span></span>

    ```Shell
    dotnet user-secrets set ngrokUrl "YOUR_NGROK_URL_HERE"
    ```

    > [!IMPORTANT]
    > <span data-ttu-id="02afc-155">Если вы перезапустите ngrok, вам потребуется повторить эту команду, чтобы обновить URL-адрес ngrok.</span><span class="sxs-lookup"><span data-stu-id="02afc-155">If you restart ngrok, you will need to repeat this command to update your ngrok URL.</span></span>

1. <span data-ttu-id="02afc-156">Измените текущий каталог в CLI на **каталог ./GraphTutorial** и запустите следующую команду, чтобы запустить функцию Azure локально.</span><span class="sxs-lookup"><span data-stu-id="02afc-156">Change the current directory in your CLI to the **./GraphTutorial** directory and run the following command to start the Azure Function locally.</span></span>

    ```Shell
    func start
    ```

1. <span data-ttu-id="02afc-157">Обновите SPA и выберите **элемент Подписки** nav.</span><span class="sxs-lookup"><span data-stu-id="02afc-157">Refresh the SPA and select the **Subscriptions** nav item.</span></span> <span data-ttu-id="02afc-158">Введите пользовательский ИД для пользователя в Microsoft 365 организации с Exchange Online почтовым ящиком.</span><span class="sxs-lookup"><span data-stu-id="02afc-158">Enter a user ID for a user in your Microsoft 365 organization that has an Exchange Online mailbox.</span></span> <span data-ttu-id="02afc-159">Это может быть либо пользователь `id` (от Microsoft Graph), либо пользователь `userPrincipalName` .</span><span class="sxs-lookup"><span data-stu-id="02afc-159">This can either be the user's `id` (from Microsoft Graph) or the user's `userPrincipalName`.</span></span> <span data-ttu-id="02afc-160">Нажмите **кнопку Подписаться**.</span><span class="sxs-lookup"><span data-stu-id="02afc-160">Click **Subscribe**.</span></span>

1. <span data-ttu-id="02afc-161">Страница обновляет отображение новой подписки в таблице.</span><span class="sxs-lookup"><span data-stu-id="02afc-161">The page refreshes showing the new subscription in the table.</span></span>

1. <span data-ttu-id="02afc-162">Отправка электронной почты пользователю.</span><span class="sxs-lookup"><span data-stu-id="02afc-162">Send an email to the user.</span></span> <span data-ttu-id="02afc-163">Через некоторое время функция `Notify` должна быть вызвана.</span><span class="sxs-lookup"><span data-stu-id="02afc-163">After a brief time, the `Notify` function should be called.</span></span> <span data-ttu-id="02afc-164">Проверить это можно в веб-интерфейсе ngrok () или в отлаговке проекта `http://localhost:4040` Azure Function.</span><span class="sxs-lookup"><span data-stu-id="02afc-164">You can verify this in the ngrok web interface (`http://localhost:4040`) or in the debug output of the Azure Function project.</span></span>

    ```Shell
    ...
    [7/8/2020 7:33:57 PM] The following message was created:
    [7/8/2020 7:33:57 PM] Subject: Hi Megan!, ID: AAMkAGUyN2I4N2RlLTEzMTAtNDBmYy1hODdlLTY2NTQwODE2MGEwZgBGAAAAAAA2J9QH-DvMRK3pBt_8rA6nBwCuPIFjbMEkToHcVnQirM5qAAAAAAEMAACuPIFjbMEkToHcVnQirM5qAACHmpAsAAA=
    [7/8/2020 7:33:57 PM] Executed 'Notify' (Succeeded, Id=9c40af0b-e082-4418-aa3a-aee624f30e7a)
    ...
    ```

1. <span data-ttu-id="02afc-165">В тестовом приложении нажмите **кнопку Удалить** в строке таблицы для подписки.</span><span class="sxs-lookup"><span data-stu-id="02afc-165">In the test app, click **Delete** in the table row for the subscription.</span></span> <span data-ttu-id="02afc-166">Страница обновляется, а подписка больше не находится в таблице.</span><span class="sxs-lookup"><span data-stu-id="02afc-166">The page refreshes and the subscription is no longer in the table.</span></span>
