---
ms.openlocfilehash: eb227079656e2a57550511c3abfacb49935fe46a
ms.sourcegitcommit: 141fe5c30dea84029ef61cf82558c35f2a744b65
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 12/11/2020
ms.locfileid: "49655242"
---
<!-- markdownlint-disable MD002 MD041 -->

<span data-ttu-id="b756c-101">В этом упражнении вы завершите реализацию функций Azure и обновим тестового приложения, чтобы подписаться и отписаться от изменений в почтовом ящике `SetSubscription` `Notify` пользователя.</span><span class="sxs-lookup"><span data-stu-id="b756c-101">In this exercise you will finish implementing the Azure Functions `SetSubscription` and `Notify`, and update the test application to subscribe and unsubscribe to changes in a user's inbox.</span></span>

- <span data-ttu-id="b756c-102">Функция будет действовать в качестве API, позволяя тестовой приложению создавать или удалять подписку на изменения в почтовом ящике `SetSubscription` пользователя. [](https://docs.microsoft.com/graph/webhooks)</span><span class="sxs-lookup"><span data-stu-id="b756c-102">The `SetSubscription` function will act as an API, allowing the test app to create or delete a [subscription](https://docs.microsoft.com/graph/webhooks) to changes in a user's inbox.</span></span>
- <span data-ttu-id="b756c-103">Функция `Notify` будет выступать в качестве веб-hook, который получает уведомления об изменениях, созданные подпиской.</span><span class="sxs-lookup"><span data-stu-id="b756c-103">The `Notify` function will act as the webhook that receives change notifications generated by the subscription.</span></span>

<span data-ttu-id="b756c-104">Обе функции будут использовать [поток предоставления](https://docs.microsoft.com/azure/active-directory/develop/v2-oauth2-client-creds-grant-flow) учетных данных клиента для получения маркера только приложения для вызова Microsoft Graph.</span><span class="sxs-lookup"><span data-stu-id="b756c-104">Both functions will use the [client credentials grant flow](https://docs.microsoft.com/azure/active-directory/develop/v2-oauth2-client-creds-grant-flow) to get an app-only token to call Microsoft Graph.</span></span> <span data-ttu-id="b756c-105">Так как администратор предоставил согласие администратора на требуемую область разрешений, для получения маркера не потребуется никаких взаимодействий с пользователем.</span><span class="sxs-lookup"><span data-stu-id="b756c-105">Because an administrator granted admin consent to the required permission scopes, no user interaction will be required to get the token.</span></span>

## <a name="add-client-credentials-authentication-to-the-azure-functions-project"></a><span data-ttu-id="b756c-106">Добавление проверки подлинности учетных данных клиента в проект функций Azure</span><span class="sxs-lookup"><span data-stu-id="b756c-106">Add client credentials authentication to the Azure Functions project</span></span>

<span data-ttu-id="b756c-107">В этом разделе вы реализуете поток учетных данных клиента в проекте функций Azure, чтобы получить маркер доступа, совместимый с Microsoft Graph.</span><span class="sxs-lookup"><span data-stu-id="b756c-107">In this section you'll implement the client credentials flow in the Azure Functions project to get an access token compatible with Microsoft Graph.</span></span>

1. <span data-ttu-id="b756c-108">Откройте CLI в каталоге, который содержит **GraphTutorial.csproj.**</span><span class="sxs-lookup"><span data-stu-id="b756c-108">Open your CLI in the directory that contains **GraphTutorial.csproj**.</span></span>

1. <span data-ttu-id="b756c-109">Добавьте свой ИД и секрет приложения веб-ook в хранилище секретов с помощью следующих команд.</span><span class="sxs-lookup"><span data-stu-id="b756c-109">Add your webhook application ID and secret to the secret store using the following commands.</span></span> <span data-ttu-id="b756c-110">Замените `YOUR_WEBHOOK_APP_ID_HERE` его на ИД приложения для веб-hook **функции Graph Azure.**</span><span class="sxs-lookup"><span data-stu-id="b756c-110">Replace `YOUR_WEBHOOK_APP_ID_HERE` with the application ID for the **Graph Azure Function Webhook**.</span></span> <span data-ttu-id="b756c-111">Замените `YOUR_WEBHOOK_APP_SECRET_HERE` секрет приложения, созданный на портале Azure для веб-приемка функции **Graph Azure.**</span><span class="sxs-lookup"><span data-stu-id="b756c-111">Replace `YOUR_WEBHOOK_APP_SECRET_HERE` with the application secret you created in the Azure portal for the **Graph Azure Function Webhook**.</span></span>

    ```Shell
    dotnet user-secrets set webHookId "YOUR_WEBHOOK_APP_ID_HERE"
    dotnet user-secrets set webHookSecret "YOUR_WEBHOOK_APP_SECRET_HERE"
    ```

### <a name="create-a-client-credentials-authentication-provider"></a><span data-ttu-id="b756c-112">Создание поставщика проверки подлинности учетных данных клиента</span><span class="sxs-lookup"><span data-stu-id="b756c-112">Create a client credentials authentication provider</span></span>

1. <span data-ttu-id="b756c-113">Создайте файл **./GraphTutorial/Authentication** с именем ClientCredentialsAuthProvider.cs и **добавьте** следующий код.</span><span class="sxs-lookup"><span data-stu-id="b756c-113">Create a new file in the **./GraphTutorial/Authentication** directory named **ClientCredentialsAuthProvider.cs** and add the following code.</span></span>

    :::code language="csharp" source="../demo/GraphTutorial/Authentication/ClientCredentialsAuthProvider.cs" id="AuthProviderSnippet":::

<span data-ttu-id="b756c-114">Удумайте, что делает код **в** ClientCredentialsAuthProvider.cs.</span><span class="sxs-lookup"><span data-stu-id="b756c-114">Take a moment to consider what the code in **ClientCredentialsAuthProvider.cs** does.</span></span>

- <span data-ttu-id="b756c-115">В конструкторе он инициализирует **ConfidentialClientApplication** из `Microsoft.Identity.Client` пакета.</span><span class="sxs-lookup"><span data-stu-id="b756c-115">In the constructor, it initializes a **ConfidentialClientApplication** from the `Microsoft.Identity.Client` package.</span></span> <span data-ttu-id="b756c-116">Он использует и функции, чтобы ограничить аудиторию входа только указанной `WithAuthority(AadAuthorityAudience.AzureAdMyOrg, true)` `.WithTenantId(tenantId)` организацией Microsoft 365.</span><span class="sxs-lookup"><span data-stu-id="b756c-116">It uses the `WithAuthority(AadAuthorityAudience.AzureAdMyOrg, true)` and `.WithTenantId(tenantId)` functions to restrict the login audience to only the specified Microsoft 365 organization.</span></span>
- <span data-ttu-id="b756c-117">В `GetAccessToken` функции она вызывает `AcquireTokenForClient` получение маркера для приложения.</span><span class="sxs-lookup"><span data-stu-id="b756c-117">In the `GetAccessToken` function, it calls `AcquireTokenForClient` to get a token for the application.</span></span> <span data-ttu-id="b756c-118">Поток маркера учетных данных клиента всегда не является интерактивным.</span><span class="sxs-lookup"><span data-stu-id="b756c-118">The client credentials token flow is always non-interactive.</span></span>
- <span data-ttu-id="b756c-119">Он реализует интерфейс, позволяя передавать этот класс в конструкторе для проверки подлинности исходяющих `Microsoft.Graph.IAuthenticationProvider` `GraphServiceClient` запросов.</span><span class="sxs-lookup"><span data-stu-id="b756c-119">It implements the `Microsoft.Graph.IAuthenticationProvider` interface, allowing this class to be passed in the constructor of the `GraphServiceClient` to authenticate outgoing requests.</span></span>

## <a name="update-graphclientservice"></a><span data-ttu-id="b756c-120">Обновление GraphClientService</span><span class="sxs-lookup"><span data-stu-id="b756c-120">Update GraphClientService</span></span>

1. <span data-ttu-id="b756c-121">Откройте **GraphClientService.cs** и добавьте в класс следующее свойство.</span><span class="sxs-lookup"><span data-stu-id="b756c-121">Open **GraphClientService.cs** and add the following property to the class.</span></span>

    :::code language="csharp" source="../demo/GraphTutorial/Services/GraphClientService.cs" id="AppGraphClientMembers":::

1. <span data-ttu-id="b756c-122">Замените имеющуюся функцию `GetAppGraphClient` указанным ниже кодом.</span><span class="sxs-lookup"><span data-stu-id="b756c-122">Replace the existing `GetAppGraphClient` function with the following.</span></span>

    :::code language="csharp" source="../demo/GraphTutorial/Services/GraphClientService.cs" id="AppGraphClientFunctions":::

## <a name="implement-notify-function"></a><span data-ttu-id="b756c-123">Реализация функции Notify</span><span class="sxs-lookup"><span data-stu-id="b756c-123">Implement Notify function</span></span>

<span data-ttu-id="b756c-124">В этом разделе вы реализуем функцию, которая будет использоваться в качестве `Notify` URL-адреса уведомлений об изменениях.</span><span class="sxs-lookup"><span data-stu-id="b756c-124">In this section you'll implement the `Notify` function, which will be used as the notification URL for change notifications.</span></span>

1. <span data-ttu-id="b756c-125">Создайте каталог в **каталоге GraphTutorials** с именем **Models.**</span><span class="sxs-lookup"><span data-stu-id="b756c-125">Create a new directory in the **GraphTutorials** directory named **Models**.</span></span>

1. <span data-ttu-id="b756c-126">Создайте новый файл в **каталоге Models** с **именем ResourceData.cs** и добавьте следующий код.</span><span class="sxs-lookup"><span data-stu-id="b756c-126">Create a new file in the **Models** directory named **ResourceData.cs** and add the following code.</span></span>

    :::code language="csharp" source="../demo/GraphTutorial/Models/ResourceData.cs" id="ResourceDataSnippet":::

1. <span data-ttu-id="b756c-127">Создайте новый файл в **каталоге Models** с **именем ChangeNotification.cs** и добавьте следующий код.</span><span class="sxs-lookup"><span data-stu-id="b756c-127">Create a new file in the **Models** directory named **ChangeNotification.cs** and add the following code.</span></span>

    :::code language="csharp" source="../demo/GraphTutorial/Models/ChangeNotification.cs" id="ChangeNotificationSnippet":::

1. <span data-ttu-id="b756c-128">Создайте новый файл в **каталоге Models** с **именем NotificationList.cs** и добавьте следующий код.</span><span class="sxs-lookup"><span data-stu-id="b756c-128">Create a new file in the **Models** directory named **NotificationList.cs** and add the following code.</span></span>

    :::code language="csharp" source="../demo/GraphTutorial/Models/NotificationList.cs" id="NotificationListSnippet":::

1. <span data-ttu-id="b756c-129">Откройте **./GraphTutorial/Notify.cs** и замените все его содержимое следующим:</span><span class="sxs-lookup"><span data-stu-id="b756c-129">Open **./GraphTutorial/Notify.cs** and replace its entire contents with the following.</span></span>

    :::code language="csharp" source="../demo/GraphTutorial/Notify.cs" id="NotifySnippet":::

<span data-ttu-id="b756c-130">Удумайте, что делает код **в** Notify.cs.</span><span class="sxs-lookup"><span data-stu-id="b756c-130">Take a moment to consider what the code in **Notify.cs** does.</span></span>

- <span data-ttu-id="b756c-131">Функция `Run` проверяет наличие параметра `validationToken` запроса.</span><span class="sxs-lookup"><span data-stu-id="b756c-131">The `Run` function checks for the presence of a `validationToken` query parameter.</span></span> <span data-ttu-id="b756c-132">Если этот параметр присутствует, он обрабатывает [](https://docs.microsoft.com/graph/webhooks#notification-endpoint-validation)запрос как запрос проверки и отвечает соответствующим образом.</span><span class="sxs-lookup"><span data-stu-id="b756c-132">If that parameter is present, it processes the request as a [validation request](https://docs.microsoft.com/graph/webhooks#notification-endpoint-validation), and responds accordingly.</span></span>
- <span data-ttu-id="b756c-133">Если запрос не является запросом на проверку, полезной нагрузки JSON десериализации в `NotificationList` .</span><span class="sxs-lookup"><span data-stu-id="b756c-133">If the request is not a validation request, the JSON payload is deserialized into a `NotificationList`.</span></span>
- <span data-ttu-id="b756c-134">Каждое уведомление в списке проверяется на наличие ожидаемого значения состояния клиента и обрабатывается.</span><span class="sxs-lookup"><span data-stu-id="b756c-134">Each notification in the list is checked for the expected client state value, and is processed.</span></span>
- <span data-ttu-id="b756c-135">Сообщение, которое вызвало уведомление, извлекается с помощью Microsoft Graph.</span><span class="sxs-lookup"><span data-stu-id="b756c-135">The message that triggered the notification is retrieved with Microsoft Graph.</span></span>

## <a name="implement-setsubscription-function"></a><span data-ttu-id="b756c-136">Реализация функции SetSubscription</span><span class="sxs-lookup"><span data-stu-id="b756c-136">Implement SetSubscription function</span></span>

<span data-ttu-id="b756c-137">В этом разделе вы реализуете функцию SetSubscription.</span><span class="sxs-lookup"><span data-stu-id="b756c-137">In this section, you'll implement the SetSubscription function.</span></span> <span data-ttu-id="b756c-138">Эта функция будет выступать в качестве API, который будет вызван тестовой приложением для создания или удаления подписки в почтовом ящике пользователя.</span><span class="sxs-lookup"><span data-stu-id="b756c-138">This function will act as an API that is called by the test application to create or delete a subscription on a user's inbox.</span></span>

1. <span data-ttu-id="b756c-139">Создайте новый файл в **каталоге Models** с **именем SetSubscriptionPayload.cs** и добавьте следующий код.</span><span class="sxs-lookup"><span data-stu-id="b756c-139">Create a new file in the **Models** directory named **SetSubscriptionPayload.cs** and add the following code.</span></span>

    :::code language="csharp" source="../demo/GraphTutorial/Models/SetSubscriptionPayload.cs" id="SetSubscriptionPayloadSnippet":::

1. <span data-ttu-id="b756c-140">Откройте **./GraphTutorial/SetSubscription.cs** и замените все его содержимое следующим:</span><span class="sxs-lookup"><span data-stu-id="b756c-140">Open **./GraphTutorial/SetSubscription.cs** and replace its entire contents with the following.</span></span>

    :::code language="csharp" source="../demo/GraphTutorial/SetSubscription.cs" id="SetSubscriptionSnippet":::

<span data-ttu-id="b756c-141">Удумайте, что делает код **в** SetSubscription.cs.</span><span class="sxs-lookup"><span data-stu-id="b756c-141">Take a moment to consider what the code in **SetSubscription.cs** does.</span></span>

- <span data-ttu-id="b756c-142">Функция считывает данные JSON, отправленные в запросе POST, чтобы определить тип запроса (подписку или отписаться), ИД пользователя, на который необходимо подписаться, и ИД подписки, для которой необходимо `Run` отписаться.</span><span class="sxs-lookup"><span data-stu-id="b756c-142">The `Run` function reads the JSON payload sent in the POST request to determine the request type (subscribe or unsubscribe), the user ID to subscribe for, and the subscription ID to unsubscribe.</span></span>
- <span data-ttu-id="b756c-143">Если запрос является запросом на подписку, он использует Microsoft Graph SDK для создания новой подписки в почтовом ящике указанного пользователя.</span><span class="sxs-lookup"><span data-stu-id="b756c-143">If the request is a subscribe request, it uses the Microsoft Graph SDK to create a new subscription in the specified user's inbox.</span></span> <span data-ttu-id="b756c-144">Подписка будет уведомлять о том, что сообщения созданы или обновлены.</span><span class="sxs-lookup"><span data-stu-id="b756c-144">The subscription will notify when messages are created or updated.</span></span> <span data-ttu-id="b756c-145">Новая подписка возвращается в полезной нагрузке ответа JSON.</span><span class="sxs-lookup"><span data-stu-id="b756c-145">The new subscription is returned in the JSON payload of the response.</span></span>
- <span data-ttu-id="b756c-146">Если запрос является запросом на отписку, он использует microsoft Graph SDK для удаления указанной подписки.</span><span class="sxs-lookup"><span data-stu-id="b756c-146">If the request is an unsubscribe request, it uses the Microsoft Graph SDK to delete the specified subscription.</span></span>

## <a name="call-setsubscription-from-the-test-app"></a><span data-ttu-id="b756c-147">Вызов SetSubscription из тестового приложения</span><span class="sxs-lookup"><span data-stu-id="b756c-147">Call SetSubscription from the test app</span></span>

<span data-ttu-id="b756c-148">В этом разделе вы реализуйте функции для создания и удаления подписок в тестовом приложении.</span><span class="sxs-lookup"><span data-stu-id="b756c-148">In this section, you'll implement functions to create and delete subscriptions in the test app.</span></span>

1. <span data-ttu-id="b756c-149">Откройте **./TestClient/azurefunctions.js** добавьте следующую функцию.</span><span class="sxs-lookup"><span data-stu-id="b756c-149">Open **./TestClient/azurefunctions.js** and add the following function.</span></span>

    :::code language="javascript" source="../demo/TestClient/azurefunctions.js" id="createSubscriptionSnippet":::

    <span data-ttu-id="b756c-150">Этот код вызывает функцию Azure для подписки и добавляет новую подписку в массив `SetSubscription` подписок в сеансе.</span><span class="sxs-lookup"><span data-stu-id="b756c-150">This code calls the `SetSubscription` Azure Function to subscribe and adds the new subscription to the array of subscriptions in the session.</span></span>

1. <span data-ttu-id="b756c-151">Добавьте вazurefunctions.js **следующую функцию.**</span><span class="sxs-lookup"><span data-stu-id="b756c-151">Add the following function to **azurefunctions.js**.</span></span>

    :::code language="javascript" source="../demo/TestClient/azurefunctions.js" id="deleteSubscriptionSnippet":::

    <span data-ttu-id="b756c-152">Этот код вызывает функцию Azure для удаления подписки из массива подписок в `SetSubscription` сеансе.</span><span class="sxs-lookup"><span data-stu-id="b756c-152">This code calls the `SetSubscription` Azure Function to un and removes the subscription from the array of subscriptions in the session.</span></span>

1. <span data-ttu-id="b756c-153">Если у вас нет запущенного ngrok, запустите ngrok ( ) и `ngrok http 7071` скопируйте URL-адрес переадрит HTTPS.</span><span class="sxs-lookup"><span data-stu-id="b756c-153">If you do not have ngrok running, run ngrok (`ngrok http 7071`) and copy the HTTPS forwarding URL.</span></span>

1. <span data-ttu-id="b756c-154">Добавьте URL-адрес ngrok в хранилище секретов пользователя, вынеся следующую команду.</span><span class="sxs-lookup"><span data-stu-id="b756c-154">Add the ngrok URL to the user secrets store by running the following command.</span></span>

    ```Shell
    dotnet user-secrets set ngrokUrl "YOUR_NGROK_URL_HERE"
    ```

    > [!IMPORTANT]
    > <span data-ttu-id="b756c-155">Если вы перезапустите ngrok, вам потребуется повторить эту команду, чтобы обновить URL-адрес ngrok.</span><span class="sxs-lookup"><span data-stu-id="b756c-155">If you restart ngrok, you will need to repeat this command to update your ngrok URL.</span></span>

1. <span data-ttu-id="b756c-156">Измените текущий каталог в CLI на **./GraphTutorial** и запустите следующую команду, чтобы запустить функцию Azure локально.</span><span class="sxs-lookup"><span data-stu-id="b756c-156">Change the current directory in your CLI to the **./GraphTutorial** directory and run the following command to start the Azure Function locally.</span></span>

    ```Shell
    func start
    ```

1. <span data-ttu-id="b756c-157">Обновите SPA и выберите элемент **nav subscriptions.**</span><span class="sxs-lookup"><span data-stu-id="b756c-157">Refresh the SPA and select the **Subscriptions** nav item.</span></span> <span data-ttu-id="b756c-158">Введите ИД пользователя в организации Microsoft 365 с почтовым ящиком Exchange Online.</span><span class="sxs-lookup"><span data-stu-id="b756c-158">Enter a user ID for a user in your Microsoft 365 organization that has an Exchange Online mailbox.</span></span> <span data-ttu-id="b756c-159">Это может быть пользователь `id` (из Microsoft Graph) или пользователь `userPrincipalName` .</span><span class="sxs-lookup"><span data-stu-id="b756c-159">This can either be the user's `id` (from Microsoft Graph) or the user's `userPrincipalName`.</span></span> <span data-ttu-id="b756c-160">Нажмите **кнопку "Подписаться"**.</span><span class="sxs-lookup"><span data-stu-id="b756c-160">Click **Subscribe**.</span></span>

1. <span data-ttu-id="b756c-161">Страница обновляется с отображением новой подписки в таблице.</span><span class="sxs-lookup"><span data-stu-id="b756c-161">The page refreshes showing the new subscription in the table.</span></span>

1. <span data-ttu-id="b756c-162">Отправьте пользователю сообщение электронной почты.</span><span class="sxs-lookup"><span data-stu-id="b756c-162">Send an email to the user.</span></span> <span data-ttu-id="b756c-163">Через некоторое время необходимо `Notify` будет вызвана функция.</span><span class="sxs-lookup"><span data-stu-id="b756c-163">After a brief time, the `Notify` function should be called.</span></span> <span data-ttu-id="b756c-164">Вы можете проверить это в веб-интерфейсе ngrok ( ) или в выходных данных отлаки `http://localhost:4040` проекта функции Azure.</span><span class="sxs-lookup"><span data-stu-id="b756c-164">You can verify this in the ngrok web interface (`http://localhost:4040`) or in the debug output of the Azure Function project.</span></span>

    ```Shell
    ...
    [7/8/2020 7:33:57 PM] The following message was created:
    [7/8/2020 7:33:57 PM] Subject: Hi Megan!, ID: AAMkAGUyN2I4N2RlLTEzMTAtNDBmYy1hODdlLTY2NTQwODE2MGEwZgBGAAAAAAA2J9QH-DvMRK3pBt_8rA6nBwCuPIFjbMEkToHcVnQirM5qAAAAAAEMAACuPIFjbMEkToHcVnQirM5qAACHmpAsAAA=
    [7/8/2020 7:33:57 PM] Executed 'Notify' (Succeeded, Id=9c40af0b-e082-4418-aa3a-aee624f30e7a)
    ...
    ```

1. <span data-ttu-id="b756c-165">В тестовом приложении **щелкните "Удалить"** в строке таблицы для подписки.</span><span class="sxs-lookup"><span data-stu-id="b756c-165">In the test app, click **Delete** in the table row for the subscription.</span></span> <span data-ttu-id="b756c-166">Страница обновляется, и подписка больше не находится в таблице.</span><span class="sxs-lookup"><span data-stu-id="b756c-166">The page refreshes and the subscription is no longer in the table.</span></span>
